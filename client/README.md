# Waypoint Client

Vite + React UI for Waypoint.

## Run locally

From the repo root:
1) `cd client`
2) `npm install`
3) Optional: set `VITE_API_BASE` in `client/.env` or `client/.env.local`
4) `npm run dev`

The client defaults to `http://127.0.0.1:8000` if `VITE_API_BASE` is not set.
